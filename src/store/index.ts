// TODO： 配置 vuex 文件
import { createStore } from 'vuex'

const state = {
  num: 1
}

const mutations = {}

const actions = {}

const modules = {}

export default createStore({
  state,
  mutations,
  actions,
  modules
})